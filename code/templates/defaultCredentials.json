{
    "huawei": {
        "admin": "Hu@wei123",
        ""

    
    "}


}




defaultCredentials = GetDefaultCredentials(napalmDriverName)

    for credential in defaultCredentials:
        try:
            defaultRemoteDevice = {
                'device_type': bestMatch,
                'host': ipAddress,
                'username': username,
                'password': password }

            connection = ConnectHandler(**defaultRemoteDevice)

            
        
        except Exception as ex:
            #check if invalid credential, else throw
            print("checkujeme")